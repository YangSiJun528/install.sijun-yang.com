{
  "name": "install-sijun-yang-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run test:config && npm run test:unit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "npm run test:unit",
    "test:config": "node --test config.test.js",
    "test:unit": "node --test worker.test.js"
  },
  "dependencies": {
    "hono": "^4.12.30",
    "url-template": "^3.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
